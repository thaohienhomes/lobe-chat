// Empty module stub used to replace heavy client-side dependencies
// (e.g. mermaid) that are not needed in the webpack client bundle.
// This prevents webpack from bundling the full dependency tree.
module.exports = {};
